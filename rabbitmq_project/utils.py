def download_corpus(folder_path):
    print(f"Скачиваю корпус в папку {folder_path}...")
    # Пока заглушка - просто возвращаем тестовый файл
    return ["test_file.txt"]

def preprocess_file(file_path):
    print(f"Обрабатываю файл {file_path}...")
    return "обработанный текст"

def find_sentences_with_word(text, word):
    print(f"Ищу слово '{word}' в тексте...")
    return ["найдено предложение 1", "найдено предложение 2"]